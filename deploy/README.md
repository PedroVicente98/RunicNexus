# deploy

This directory contains Kubernetes manifests and Docker configuration
files for deploying RunicNexus to a container orchestration platform.

```
deploy/
├── docker/          # Dockerfiles for each service
└── kubernetes/      # Helm charts and raw K8s manifests
```
